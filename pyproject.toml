[project]
name = "openai-agent-sdk-realtime"
version = "0.1.0"
description = "Realtime voice multi-agent example using the OpenAI Agents SDK"
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.15.1",
    "python-dotenv>=1.0.0",
    "pyaudio>=0.2.14",
    "numpy>=2.0.0",
]
